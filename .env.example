# Focus Bot Configuration
# Copy this file to .env and fill in your values

# Required: Bot token from @BotFather on Telegram
# Message @BotFather, send /newbot, follow prompts, copy the token
TELEGRAM_BOT_TOKEN=your-token-from-botfather

# Required: Comma-separated Telegram user IDs allowed to use the bot
# Message @userinfobot on Telegram to get your user ID
ALLOWED_USER_IDS=123456789,987654321

# Required: Absolute path to your Obsidian vault (or notes directory)
# This is where captured messages will be saved as markdown files
NOTES_DIR=/absolute/path/to/obsidian/vault

# Optional: Anthropic API key for Claude
# If not set, uses Claude subscription model (requires active subscription)
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional: Claude model for note capture metadata extraction
# Default: haiku (fast, cheap). Other options: sonnet, opus
CAPTURE_MODEL=haiku

# Optional: Claude model for async enrichment (YouTube summaries, etc.)
# Default: haiku. Other options: sonnet, opus
ENRICHMENT_MODEL=opus

# Optional: Path to transcript debug log (plain text, one entry per voice message)
# Default: /tmp/focus-bot-transcripts.log
# TRANSCRIPT_LOG=/tmp/focus-bot-transcripts.log

# Optional: Subdirectory name (relative to NOTES_DIR) for user-configurable prompts
# When set, Focus Bot seeds default prompt files in NOTES_DIR/<value>/Focus Bot/
# Edit the Markdown files to customize the bot's AI behavior in real-time
# Omit to use built-in defaults
# PROMPTS_DIR=Prompts

# Optional: Path to claude CLI executable
# Default: 'claude' (resolved via system PATH)
# CLAUDE_CODE_PATH=/usr/local/bin/claude
